services:
  - type: worker
    name: apecoin-airdrop-bot
    env: python
    region: frankfurt

    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py

    envVars:
      - key: TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.2
